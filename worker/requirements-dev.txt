# Development dependencies for the worker
# This file includes all runtime dependencies plus development tools

# Include all runtime dependencies
-r requirements.txt

# Development and testing tools (minimal for now)
